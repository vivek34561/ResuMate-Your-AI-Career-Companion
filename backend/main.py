"""
Deprecated: FastAPI backend has been removed. This module is kept only to
prevent import errors in environments referencing backend.main.

Use the Streamlit app entrypoint (app.py) instead.
"""

raise RuntimeError("FastAPI backend removed. Use Streamlit app (app.py) only.")
